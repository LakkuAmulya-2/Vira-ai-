# Phase 10 Vira AI Copilot

The copilot is a persistent student operating layer above the agent runtime.

It combines student intelligence, memory, applications and multi-agent reasoning into one conversation. Actions are proposed separately from execution. High-impact external actions remain confirmation-gated and no external application submission is performed by this phase.

Core loop: context -> supervisor/agents -> answer -> proposed actions -> explicit confirmation -> future executor.
