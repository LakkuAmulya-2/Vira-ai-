# Security Boundaries

Authentication identifies the caller. Authorization must additionally enforce ownership for student-scoped resources. Sensitive external actions remain confirmation-gated. Audit events record security-relevant transitions without storing secrets.
