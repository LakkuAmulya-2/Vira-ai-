"use client";
import {FormEvent,useMemo,useState} from "react";import {useRouter} from "next/navigation";import {apiFetch} from "../../lib/api/client";
const questions=[{key:"education_stage",label:"Where are you right now?",placeholder:"After 10th, After 12th, Undergraduate..."},{key:"interests",label:"What genuinely interests you?",placeholder:"Technology, design, biology..."},{key:"career_goals",label:"What would you like to explore?",placeholder:"Building products, research, helping people..."},{key:"preferred_countries",label:"Where are you open to studying?",placeholder:"India, Germany, UK..."},{key:"budget",label:"What annual budget should we respect?",placeholder:"Enter amount in your local currency"}];
export default function OnboardingPage(){const router=useRouter();const [index,setIndex]=useState(0),[answers,setAnswers]=useState<Record<string,string>>({}),[loading,setLoading]=useState(false),[error,setError]=useState("");const current=questions[index],progress=useMemo(()=>((index+1)/questions.length)*100,[index]);
async function submit(e:FormEvent){e.preventDefault();if(!answers[current.key]?.trim())return;if(index<questions.length-1){setIndex(index+1);return}setLoading(true);setError("");try{await apiFetch("/api/v1/student-intelligence/profile",{method:"POST",body:JSON.stringify({education_stage:answers.education_stage,country_code:"IN",interests:(answers.interests||"").split(",").map(x=>x.trim()).filter(Boolean),career_goals:(answers.career_goals||"").split(",").map(x=>x.trim()).filter(Boolean),preferred_countries:(answers.preferred_countries||"").split(",").map(x=>x.trim()).filter(Boolean),annual_budget_minor:Number(answers.budget)||undefined})});router.push("/dashboard")}catch(err){setError(err instanceof Error?err.message:"Unable to save profile")}finally{setLoading(false)}}
return <main className="onboarding"><div className="top"><a href="/">← VIRA</a><span>{index+1} / {questions.length}</span></div><div className="progress"><i style={{width:progress+"%"}}/></div><form onSubmit={submit} className="question"><div className="eyebrow">STUDENT INTELLIGENCE</div><h1>{current.label}</h1><p>There are no right answers. Vira uses your context to make better decisions with you.</p><textarea autoFocus value={answers[current.key]||""} placeholder={current.placeholder} onChange={e=>setAnswers({...answers,[current.key]:e.target.value})}/>{error&&<div className="notice error">{error}</div>}<div className="form-actions"><button type="button" disabled={index===0||loading} onClick={()=>setIndex(index-1)}>Back</button><button className="primary" disabled={loading}>{loading?"Building profile…":index===questions.length-1?"Build my profile →":"Continue →"}</button></div></form></main>}
