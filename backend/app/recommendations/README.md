# Phase 7 Decision Intelligence

Pipeline: verified candidates -> hard eligibility filters -> personalized scoring -> confidence -> tiering -> persisted run -> explainable result.

The engine never claims guaranteed admission, scholarship or eligibility. Missing profile information lowers personalization quality rather than being silently fabricated.
