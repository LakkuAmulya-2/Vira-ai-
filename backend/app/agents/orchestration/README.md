# Phase 5 Agentic Runtime

The orchestrator is evidence-first.

Supervisor -> explicit plan -> least-privilege tools -> specialist execution -> result validation -> persisted trace.

No specialist receives unrestricted database access. Tools are allowlisted per agent. High-impact recommendation, admission, scholarship and exam results are flagged for human review when evidence is absent.
