# Source Verifier Worker

Reserved production worker boundary. It must fetch only allowlisted sources, detect document changes, register content hashes, and move records through verification policies. It does not fabricate facts.
